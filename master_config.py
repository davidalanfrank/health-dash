MASTER_PARAMS = {
    "dex_test_file":'data/dexa/dexa_1.pdf',
    "dex_prompt_1":'Aggregate this data taken from a DEXA scan pdf. Once we have the results, transform them into a json object that can be parsed easily',
}
